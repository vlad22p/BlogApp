import React from "react";
import { withRouter } from "react-router-dom";

function PostsComponent() {
    return (
        <div>this will be the posts component</div>
    )
}

export default withRouter(PostsComponent);